<div class="container" id="container-with-navbar">
  <div class="title">
    <h3><%= @balcony.title %></h3>
    <p><strong><%= @balcony_plants.where(planted: true).count %></strong> plantes sur ton balcon</p>
  </div>

  <% if @balcony_plants.present? %>
    <%# <%=  render "suggested_plants" %>
    <%= render "balcony_plants" %>
  <% else %>
    <%= render "suggested_plants" %>
  <% end %>
  
</div>

