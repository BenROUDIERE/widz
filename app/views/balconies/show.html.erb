<div class="container" id="container-with-navbar">
  <div class="title">
    <h4><%= @balcony.title %></h4>
  </div>

  <% if @balcony_plants.present? %>
    <%= render "suggested_plants" %>
    <%# <%= render "balcony_plants" %>
  <% else %>
    <%= render "suggested_plants" %>
  <% end %>
</div>
